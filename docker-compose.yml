version: '3.8'

services:
  mis-ticketing-v1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mis-ticketing-v1
    ports:
      - "9010:9010"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: 1
    restart: unless-stopped